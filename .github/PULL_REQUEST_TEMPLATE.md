**What changed and why**


**How you tested it**


- [ ] Every commit is signed off (`git commit -s`)
- [ ] `npm test`, `npm run lint` and `npx tsc --noEmit` pass
- [ ] Prisma queries stay workspace-scoped; user-supplied URLs and SQL go through the guards
